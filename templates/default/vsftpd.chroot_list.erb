# chroot_list user file for vsftpd
# Written by Chef
# Changes will be overwritten after next
# chef-client run

<% if node['vsftpd']['chroot'].to_a.empty? or node['vsftpd']['chroot'].nil? %>
       <% node['vsftpd']['chroot'].each do |user| %>
<%= user %>
       <% end %>
<% end %>
